<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Todo List APP test</title>
       <style>

    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #0d021f, #1a0134, #3a015c);
        color: #e5d6f0;
        margin: 0;
        padding: 0;
        transition: background 0.3s ease, color 0.3s ease;
        font-style: italic;
    }

    h1 {
        background: linear-gradient(to right, #da22ff, #9733ee);
        color: white;
        margin: 0;
        padding: 20px;
        text-align: center;
        box-shadow: 0 4px 20px rgba(154, 89, 255, 0.5);
        text-transform: uppercase;
        letter-spacing: 2px;
        transition: all 0.4s ease;
    }

    ul {
        list-style: none;
        padding: 20px;
    }

    ul li {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        margin-bottom: 15px;
        padding: 15px 20px;
        position: relative;
        border-radius: 8px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.6);


    </head>

    <body>
        <h1>SRISHTI TO-DO LIST</h1>
        <ul>
            <% todolist.forEach(function(todo, index) { %>
            <li>
                <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
                <a href="/todo/<%= index %>" class="edit-btn">✎</a>
                <%- todo %>
            </li>
            <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <p>
                <label for="newtodo">What should I do?</label>
                <input type="text" name="newtodo" id="newtodo" autofocus />
                <input type="submit" value="Add" />
            </p>
        </form>
    </body>
</html>
